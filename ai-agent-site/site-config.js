/**
 * Do NOT set IDE_PAGE_URL to https://s3.amazonaws.com/idecompiler/open
 * That host runs a different app that generates random addresses on Copy Address.
 *
 * Leave both empty to use YOUR current host (index.html → ide.html on same bucket).
 * Or set your own URLs after upload, e.g.:
 *   window.IDE_PAGE_URL = "https://s3.amazonaws.com/YOUR-BUCKET/open";
 */
window.SITE_PUBLIC_URL = "";
window.IDE_PAGE_URL = "";
