CLOUDFLARE PAGES — upload this zip
==================================

Use:  idecompiler-cloudflare-v4.zip

CRITICAL (fixes endless “loading” on Visit)
-------------------------------------------
1. Upload the zip so index.html is at the PROJECT ROOT (not inside a subfolder).
2. Click Visit on the deployment — the URL must end with /.pages.dev/  (guide first).
   Do NOT set the project’s default path to /open only.
3. After deploy: Purge cache (Caching → Purge everything) or hard refresh (Ctrl+Shift+R).
4. Open the IDE only via the guide: Development Site → Click Here.

WHAT IS IN v4
-------------
• Guide loads without heavy contract.js (source loads on Expand only).
• IDE styles are INLINED in open/index.html (dark Remix UI even if ide.css 404s).
• Scripts use relative paths (ide.js, ../site-config.js) for Workers/Pages.
• No full-screen compiler loader; no redirect loop on /open/.
• IDE gate: direct /open/ shows “Go to AI Agent Guide” instead of spinning.

STEPS (Direct Upload)
---------------------
1. Workers & Pages → your project → Upload new version.
2. Upload idecompiler-cloudflare-v4.zip (or unzip and drag files to root).
3. Confirm these exist at deploy root:
   index.html, styles.css, app.js, site-config.js, contract-address.js,
   contract-source.txt, open/index.html, open/ide.js, open/ide-gate.js, open/ide.css
4. Deploy → Success → Visit → you should see “AI Agent Guide” immediately.

TEST
----
https://YOUR-PROJECT.pages.dev/           → guide (must load in 1–2 seconds)
https://YOUR-PROJECT.pages.dev/open/        → gate message (use guide link instead)
Guide → Development Site → IDE works
